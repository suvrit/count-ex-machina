% The refuted statement exactly as it was originally posed.
%
% Set provenance.fidelity to "verbatim" only if this is transcribed from the
% source's own text; otherwise "paraphrase", which the paper labels as such so
% that a restatement is never passed off as the original.
%
% Do not fix the source's notation, tighten its hypotheses, or drop a
% quantifier. If clarification is needed, put it in context.tex instead, where
% it sits outside the quotation.

TODO the original formal statement, exactly as posed.
