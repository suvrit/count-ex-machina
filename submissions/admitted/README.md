# Admitted bundles

These bundles were unpacked into `counterexamples/<case-id>/` and admitted.
They are kept as the record of what was submitted; the case directory is the
authoritative version, and nothing here is read by `tools/build.py` or by CI.
