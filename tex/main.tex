\documentclass[11pt]{amsart}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage[inline]{enumitem}
\usepackage[authoryear,round]{natbib}
\usepackage{hyperref}
% The case format: the cx... environments each counterexamples/<id>/case.tex is
% written in, plus the theorem environments, status tags and notation they use.
\usepackage{cxcase}
\usepackage{listings}
\usepackage{graphicx}
\usepackage{url}
\usepackage{multirow}

% Every \cite in the sources is used parenthetically, so make bare \cite mean
% \citep; \citet is still available where the authors are read as prose.
\AtBeginDocument{\let\cite\citep}
\hypersetup{colorlinks=true,linkcolor=blue!45!black,citecolor=blue!45!black,urlcolor=red!65!black}
\setlist{nosep,leftmargin=*}
\lstset{basicstyle=\ttfamily\small,breaklines=true,frame=single,columns=fullflexible}

% Bold the small-caps section titles.  amsart sets them {\normalfont\scshape
% \centering} (amsart.cls l.1112); we insert \bfseries surgically rather than
% redefining \section, because cxcase.sty has already \pretocmd'd the section
% rule onto it and a redefinition here would silently throw that hook away.
%
% The \fontfamily{ppl} is the actual hack, and it is not optional: Latin Modern
% ships NO bold small caps in any family -- t1lmr.fd declares sc only in series
% m, and cfr-lm's t1clm.fd "provides" T1/clm/bx/sc as <->ssub*clm/bx/n, i.e. it
% silently gives you bold upright with no warning and no small caps.  Palatino
% has the real thing (t1ppl.fd l.26 declares T1/ppl/b/sc, and bx/sc ssubs to
% it), and ppl is already in the document -- the abstract's repo URL uses it.
% Headings are therefore Palatino while the body stays Latin Modern.
% \subsection is untouched: amsart already sets it \bfseries, and not in caps.
\makeatletter
\patchcmd{\section}{\scshape}{\fontfamily{ppl}\bfseries\scshape\selectfont}{}{%
  \PackageWarning{main}{Could not bold the section font; amsart may have changed}}
\makeatother

\numberwithin{equation}{section}   % I prefer this style

\title{GPT, the Counterexample Machine}
% amsart forbids \thanks inside \author, and its title-block footnotes are
% unmarked (amsart.cls l.655 \let\@thefnmark\relax), so the * that ties the note
% to the author name is placed by hand at both ends.  The optional argument
% keeps the star out of the running head.
\author[Count ex Machina]{Count ex Machina\textsuperscript{\normalfont*}}
\thanks{*\,Project name used to future proof for possible multiple contributors. The first version is authored by Suvrit Sra (\texttt{s.sra@tum.de}; TU Munich, Dept.~of Math, School of CIT.)}
%\email{s.sra@tum.de}
%\address{TU Munich, Dept.\ of Mathematics, School of CIT; Garching, Germany}
\date{Draft of \today}
\subjclass[2020]{Primary 68T01, 00A30; Secondary 05E05, 15A42, 26D10, 60E15, 68W40}
\keywords{AI-assisted mathematics, counterexamples, real stable polynomials, Schur positivity, matrix inequalities, determinantal point processes, reproducible mathematics}

\begin{document}
\begin{abstract}
  This document reports over 15 counterexamples found using GPT Pro over the course of 12 months, for problems in combinatorics, number theory, convexity, analysis, and other areas. The counterexamples concern both (reasonably) well-known problems as well as lesser known ones. Our counterexamples are accompanied by the repo \href{https://github.com/suvrit/count-ex-machina}{\fontfamily{ppl}\selectfont\itshape https://github.com/suvrit/count-ex-machina}, and we warmly welcome new contributions toward helping building a rich \emph{``open-source countex library.''}
\end{abstract}
\maketitle

\input{01-literature}
\input{02-admission}

%\section{Summary of counterexamples}
%\vspace{-10pt}
% tex/ledger.tex is hand-written and nothing regenerates it: edit the table
% there row by row.  `make check` only verifies that every admitted result
% still has a row, and prints the row to paste when one is missing.
\input{ledger}

\input{generated/cases}

% The closing section.  Hand-written; its \section closes the rule opened by
% the last case, which is why it sits before the acknowledgments rather than
% after them.
\input{07-conclusion}

\vspace*{1cm}
\subsection*{Acknowledgments}
The author acknowledges generous support from the Alexander von Humboldt (AvH) foundation via an AvH Professorship in AI. I'd like to also thank Apoorva Khare (IISC) for suggesting that I write up all the progress in one document, which was instrumental in helping me overcome the \emph{``writer's block''} from not wanting to write 10+ separate preprints!

\subsection*{Statement on AI Usage}
I used Claude Code (Opus 5) to architect the document and the repo. Initial drafts were assembled by Claude using some of our GPT Pro transcripts as well as from my own existing \TeX code. However, the I rewrote the whole document myself to improve clarity, exposition, and precision, while ensuring it is authored in \emph{``my own voice.''} 


\bibliographystyle{abbrvnat}
\bibliography{references}


% The list of appendices, by hand: there are enough of them that a reader cannot
% find one by paging.  Letters and pages come from \ref/\pageref so they cannot
% go stale, but the titles are typed here -- shorten one, annotate it, or drop a
% line, and only this list changes.  \cxendrule closes the last body section,
% since what follows is not a \section.
\cxendrule
% Centered small caps with a line of air above and half a line below is exactly
% what amsart gives a contents heading (\contentsnamefont is \scshape; see
% amsart.cls l.1146-1151).  Spelled out rather than borrowed so it can be
% restyled here without knowing that.
\par\addvspace{\baselineskip}
{\centering\scshape Appendices\par}
\addvspace{.5\baselineskip}
\begin{itemize}[label={},leftmargin=2.2em,itemsep=0.2em]
\item[\ref{sec:additional}.] Additional counterexamples \dotfill \pageref{sec:additional}
\item[\ref{sec:proofdetails}.] Positive results for the mixed-norm Cauchy--Schwarz inequality \dotfill \pageref{sec:proofdetails}
\item[\ref{sec:aim36pencil}.] Specht modules and Borcea--Br\"and\'en's Problems 36, 37 \dotfill \pageref{sec:aim36pencil}
\item[\ref{sec:macdonaldomega}.] Additional results for McSwiggen--Sahi Theorem 2.1 \dotfill \pageref{sec:macdonaldomega}
\item[\ref{sec:qcctrace}.] Some refinements of the quantum coupon collection conjecture \dotfill \pageref{sec:qcctrace}
\item[\ref{sec:lorentzianaudit}.] Two new three-point results for hyperbolic polynomials \dotfill \pageref{sec:lorentzianaudit}
\end{itemize}

% The appendices.  09-additional.tex issues the \appendix, so it must come
% first; each file after it only opens a \section and is numbered B, C, ...
% Reorder these lines freely -- everything points at them with Appendix~\ref,
% and the list above renumbers itself to match.
\input{09-additional}
\input{03-proof-details}
\input{04-aim36-pencil}
\input{05-macdonald-omega}
\input{06-quantum-coupon-trace}
\input{08-lorentzian-audit}
\cxendrule   % closes the last section; the others are closed by the next heading
\vskip 12pt

\end{document}
