% Appendix A -- the cases the archive keeps but does not foreground.
%
% Hand-written, and nothing regenerates it: this file owns the heading, every
% word of prose, the order of the parts, and the \input line of each dossier.
% The one generated thing in it is the list of \cxbriefitem lines, driven by the
% "brief" flag in each counterexamples/<id>/case.json.  Add prose anywhere:
% before the dossiers, between two of them, between them and the brief list,
% after.  tools/build.py only checks that every case marked "appendix": true
% (and not "brief") is \input here, and prints the line to paste when one is
% missing.
%
\section{Additional counterexamples}
\label{sec:additional}
We list some additional counterexamples below. They are not in the main body, either because by the time we wrote them  down others had also found their own counterexamples, or the statement under study did not meet all our admission criteria (e.g., public availability of the conjecture).

\cxlevel{subsection}
\input{../counterexamples/courtade-volume-conjecture/case.tex}
\input{../counterexamples/lorentzian-jensen/case.tex}
\input{../counterexamples/dpp-feasible-step/case.tex}
\input{../counterexamples/sdd-nystrom-diminishing-returns/case.tex}

\subsection{Statements recorded without a dossier}

These fell to the same standard of evidence as every case above --- each carries
a certificate in the repository that recomputes in exact arithmetic --- but the
statements are slight enough that the archive records them here rather than
spending a section and a ledger row on each.

\medskip
\begin{itemize}[itemsep=0.4em]
\input{generated/appendix-brief}
\end{itemize}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: "main"
%%% End:
