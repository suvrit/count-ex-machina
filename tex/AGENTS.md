# AGENTS.md — the paper

Scope: `tex/`. Read `/AGENTS.md` first.

## Generated vs. hand-written

| File | |
|---|---|
| `main.tex` | preamble, front matter, the `\input`s, and the list of appendices. Hand-written. |
| `01-literature.tex`, `02-admission.tex` | hand-written. `02-admission.tex` is the **authoritative** admission rule and audit standard — `CONTRIBUTING.md` summarizes it, not the other way round. |
| `cases.tex` | **the body's case list** — section headings, each case's `\cxlevel`, one `\input` per case, in order. Hand-written; `tools/build.py` only checks that every body case is `\input` there. |
| `03-…`, `04-…`, `05-…`, `06-…`, `08-…`, `09-additional.tex` | the appendices, hand-written. `09-additional.tex` is Appendix A: it owns the heading, all prose and the dossier `\input`s, and `\input`s the generated brief list. The document's only `\appendix` is issued in `main.tex`, just before the appendix `\input`s. |
| `references.bib` | hand-written; `bib_keys` in each `case.json` must resolve here. |
| `cxcase.sty` | **the case format** — the `cx...` environments every `case.tex` is written in, plus the theorem environments, status tags and notation. Hand-written; changing it changes every case. |
| `generated/metadata.tex`, `generated/appendix-brief.tex` | **generated by `tools/build.py` — never edit.** Each carries a `% GENERATED` header. Edit the case's `case.tex` or `case.json` and run `make regen`. |
| `main.pdf`, `main.aux`, `main.log`, … | build output, gitignored. |

`generated/metadata.tex` contains no prose at all: per result it declares the
metadata LaTeX cannot know — the theorem label, and whether the quoted
statement is verbatim, a paraphrase, or still `\cxpending` — keyed by result
id, so `main.tex` loads it once in the preamble. Everything typeset comes out of
the case file itself, through `cxcase.sty`. That is why a `cxrefutation` must
contain **no** heading and **no** credit line of its own: the package emits
both. Paths inside a case resolve against `tex/`.

**The case lists are hand-written.** `cases.tex` is the body: one
`\input{../counterexamples/<id>/case.tex}` per case, in the order you want
them, and nothing else — every case is a `\section` whose heading is the
`\cxtitle` in its own file, and a case that bundles several statements opens
its own subsections with `\cxsubtitle` there (`aim-problems` is the example).
Move a line to move a section; `make regen` touches none of it.
`09-additional.tex` holds the dossier
`\input`s of the `"appendix": true` cases the same way, with prose wherever
you like between them, and `\input`s `generated/appendix-brief.tex` — the bare
`\cxbriefitem` lines of the `"brief": true` cases, which stays generated
because its wording comes from each case's own `cxsummary` / `cxcertificate`.

What `tools/build.py` does with the two lists is check *coverage*, as for the
ledger: every body case must be `\input` in `cases.tex` and every appendix
dossier in `09-additional.tex`, once each, and nothing else may be — not a
withheld case, not a brief one, not one in the other file. Add a case and
`make check` prints the `\input` line to paste. Until August 2026 both lists
were generated, and two directories (`stable-schur`, `aim-problem-36`) shared a
section whose heading came from a `GROUPS` table in `build.py` through a
`\cxgroup` macro. The macro is gone, and so is the idea: the four AIM problems
are one case, `aim-problems`, and no heading lives outside a case file.

**The ledger is hand-written.** `tex/ledger.tex` holds the whole table —
`longtable` preamble, caption, rules, running header, and one row per admitted
statement — and **nothing regenerates it**. Reword a cell, resize the columns,
reorder or annotate rows freely; `make regen` will not touch a character.

What `tools/build.py` does instead is check *coverage*: every refuted result's
`theorem_label` must appear in a `\ref` somewhere in the file, and every
`\ref{thm:...}` there must name a real result. Add a case and `make check`
names the missing row and prints one you can paste. That single `\ref` is the
only tie between the table and the cases; the prose is yours. A `cxsummary` or
`cxcertificate` edit therefore does **not** propagate — update the row yourself
if you want it to.

Keep blank lines out of the table body: inside an alignment a blank line is a
`\par`, which makes the following `\bottomrule` an illegal `\noalign`. Comment
lines are fine, which is why the row separators are `%`.

Until August 2026 the ledger was assembled by LaTeX instead: each
`cxcertificate` wrote a row into the `.aux` and a `\cxledger` macro in
`cxcase.sty` typeset the previous pass's rows, so the table needed two passes
and could not be edited where it was used. Both `\cxledger` and `\cxledgerrow`
are gone, and `cxsummary` / `cxcertificate` now only delimit text for
`registry.json` and for the suggested-row message.

## Compiling

```sh
cd tex && latexmk -pdf main      # or: make paper
make clean                       # latexmk -C
```

Compile from `tex/` — the `\input` paths assume it. On Overleaf, import the
whole repository and set the main document to `tex/main.tex`.

One case on its own, while writing it: `make preview CASE=<id>`. That wraps the
case file in a minimal preamble (`tools/preview_case.py`) and leaves
`tex/preview/preview-<id>.pdf`. A preview has no metadata, so every quoted
statement is labelled a paraphrase there; `make paper` is the real thing.

Everything a preview produces stays in `tex/preview/`, which is gitignored
whole, so `tex/*.tex` is exactly the files a person writes. The wrapper
compiles from inside that directory with `TEXINPUTS`/`BIBINPUTS` pointing one
level up, which is how `cxcase.sty` and `references.bib` are still found.
Deleting the directory at any time is safe.

For arXiv: `make arxiv` (`tools/flatten_arxiv.py`) folds the whole paper into
`arxiv/main.tex`, `arxiv/appendix.tex` and `arxiv/cxcase.sty`, with every
`\input` inlined, the bibliography spliced in from a fresh bibtex run, and
whole-line comments and `\iffalse ... \fi` blocks removed, since the source
is public there. It then compiles the three files on their own and fails on an
undefined reference or citation. `arxiv/` is regenerated wholesale each run and
the whole directory is gitignored -- fix things in `tex/`, not there.

## Preamble facts worth knowing before you write LaTeX

- Bare `\cite` is `\citep` (`\AtBeginDocument{\let\cite\citep}`); use `\citet`
  where the authors are read as prose, and `\citeyearpar{...}` after a name
  already spelled out.
- **`\section` and `\subsection` are hooked.** `cxcase.sty` prepends a rule to
  each, so every section is visually closed by the one that follows it; the
  first heading is skipped, and `\cxendrule` in `main.tex` closes the last.
  Do not add rules by hand — you will get two. Tune `\cxsecruleabove`,
  `\cxsecrulebelow`, `\cxsecrulewidth`; setting the width to `0pt` turns them
  off everywhere.
- `cleveref` is loaded: `\Cref{thm:...}`.
- **Every quote box is labelled `ctx:<result-id>`** by `cxstatement` itself.
  Point at one with `Problem~\ref{ctx:aim-problem-35}` — never with the
  source's own numbering ("Problem 35"), which resolves to nothing here. The
  source's number stays visible in the box's heading.
- Status macros: `\statusfalse`, `\statusproved`, `\statusnumeric`,
  `\statusconditional`, `\statusspeculative`. A refutation's theorem uses
  `\begin{theorem}[\statusfalse: ...]`.
- Math shorthands, already defined — do not redefine: `\R`, `\C`, `\tr`,
  `\per`, `\Imm`, `\diag`, `\Vol`, `\CE`, `\J`, `\disc`.
- Environments: `theorem` (numbered per section) with `proposition`, `lemma`,
  `corollary` sharing its counter; `problem`, `definition`, `example`, `audit`;
  `remark`.
- `problem` is boxed via `tcolorbox` **on purpose**: it holds statements quoted
  as originally posed, so the source's words can never be mistaken for ours.
  Do not put our own restatement inside a `problem`.
- `\casecredits{...}` is emitted by the build from the case's `cxcredits`
  region. Do not write it by hand.
- **An appendix is a plain `\section{...}` with a `\label`, and adding one is
  two edits.** `main.tex` issues the document's only `\appendix`,
  so every appendix file `\input` after it is lettered
  automatically. Add the `\input` line, then add a line to the hand-written
  list of appendices just above it. That list takes its letters and pages from
  `\ref`/`\pageref`, so only the title is typed twice — shorten it, annotate
  it, or leave a line out on purpose. Title the appendix for the main-text
  result it supports ("Additional results for ..."), since that is all the
  list shows.

  Until August 2026 this went through `cxcase.sty`: `\cxappendixsection` and
  `\cxappendix` wrote into a private `main.apx` contents stream that
  `\cxappendixtoc` read back, with a `tex/latexmkrc` rule to make latexmk rerun
  on it. Each title was then spelled once, at the cost of a list nobody could
  edit where it was used and a heading that belonged to a generated file. All
  four macros, `main.apx` and `tex/latexmkrc` are gone.
- The `cx...` environments are defined in `cxcase.sty`, so LaTeX itself
  catches an unclosed or misspelled one. `tools/build.py` reads the same regions
  for `registry.json` and the README table — the two artifacts LaTeX cannot
  produce for a CI that has no TeX installed.

If a new macro is genuinely needed, add it to `cxcase.sty` rather than to
`main.tex` (a case would not have it under `make preview`) and never inside a
case — cases are `\input` in ledger order, so a local definition leaks to the
ones after it.
