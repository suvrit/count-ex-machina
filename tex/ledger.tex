% The audited archive ledger -- HAND-WRITTEN.  Edit it row by row.
%
% One row per admitted statement:
%
%     \cxn & Formal statement & Witness.\ \emph{<level> certificate}; human proof Thm.~\ref{<theorem_label>}.\\
%
% The certificate cell carries two things: the machine check in this repo's own
% taxonomy (\emph{Exact certificate} or \emph{Interval certificate}, matching
% case.json's certificate_level), and the human-vetted proof, cross-referenced
% to the theorem that states it.  Where the assertion rests on an enclosure
% rather than an argument a referee can follow by hand, say so instead:
%
%     ... \emph{Interval certificate}; no closed-form proof (reduction in Thm.~\ref{...}).\\
%
% \cxn is the margin number and counts itself (see cxcase.sty), so insert,
% reorder or delete rows freely -- never type a row number.  It is
% \refstepcounter-based, so a row may carry a \label and be cited from prose.
%
% Nothing regenerates this file.  Reword a cell, resize the columns, reorder,
% split, or annotate it freely; `make regen` will not touch a character.
%
% What the build does check (tools/build.py) is that no admitted result is
% missing: every refuted result's theorem_label must appear once in a \ref
% here, and every \ref here must name a real result.  Add a case and `make
% check` tells you which row to add, and prints one you can paste.  That is
% the only tie between this table and the cases -- the prose is yours.
%
% Keep a blank line out of the table body: inside an alignment it is a \par,
% which makes the following \bottomrule an illegal \noalign.  Comment lines
% starting with % are fine, which is why the separators below use them.

% The number column is pulled into the left margin by \LTleft, so the two
% content columns keep the full \linewidth.  Adjust its width and the gap here;
% \LTleft is restored after \end{longtable} so other longtables are unaffected.
\newlength{\cxnumwd}\setlength{\cxnumwd}{2.1em}
\newlength{\cxnumsep}\setlength{\cxnumsep}{0.65em}
\setlength{\LTleft}{-\dimexpr\cxnumwd+\cxnumsep\relax}
\setlength{\LTright}{\fill}
\setcounter{cxrow}{0}   % rows number themselves; see \cxn in cxcase.sty
\begin{longtable}{@{}>{\raggedleft\arraybackslash}p{\cxnumwd}@{\hspace{\cxnumsep}}p{0.46\textwidth}p{0.50\textwidth}@{}}
  \label{tab:ledger}\\
  \cmidrule[\heavyrulewidth]{2-3}
  & Formal statement & Certificate \\
  \cmidrule{2-3}
  \endfirsthead
  \cmidrule[\heavyrulewidth]{2-3}
  & Formal statement & Certificate \\
  \cmidrule{2-3}
  \endhead
%
  % aim-problem-35 (body)
  \cxn & Borcea--Br\"and\'en Problem 35: existence of Johnson polynomial representation for stable polynomials \citep{AIMStableProblems} & Stable degree-five polynomial plus size-five immanant POT obstruction.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:common-A}.\\
%
  % aim-problem-38 (body)
  \cxn & Borcea--Br\"and\'en Problem 38: Stable polynomial Permanent-On-Top \citep{AIMStableProblems} & Exact Schur expansion; four violating partitions.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:pot}.\\
%
  % aim-problem-36 (body)
  \cxn & Borcea--Br\"and\'en Problem 36: Schur positivity of symmetric stable polynomials \citeyearpar{AIMStableProblems} & Positive-definite $5\times5$ det-pencil; exact Schur expansion with $[s_{(1^5)}]q=-2972$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:aim36}.\\
%
  % aim-problem-37 (body)
  \cxn & Borcea--Br\"and\'en Problem 37: stable Schur inequality (lower endpoint) \citeyearpar{AIMStableProblems} & Same pencil; $a_{(1^5)}=-2972<1728=f^{(1^5)}a_{(5)}$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:aim37}.\\
%
  % macdonald-schur-convexity (body)
  \cxn & McSwiggen--Sahi Theorem 2.1: Macdonald lattice Schur-convexity \citeyearpar{McSwiggenSahi2026} & Rank-two Schur specialization, valid for every $0<r<1$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:macdonald}.\\
%
  % theta-derivative-log-concavity (body)
  \cxn & Csordas (Conj.~2.5) Turán inequalities for Jacobi-Theta derivatives $\Phi^{(n)}$~\citep{Csordas2014} & Rigorous interval evaluation of $J_9(1/50)<0$.\ \emph{Interval certificate}; no closed-form proof (reduction in Thm.~\ref{thm:theta}).\\
%
%
  % variance-only-matrix-discrepancy (body)
  \cxn & Variance-sensitive Matrix Spencer for $n$ contractions of size $n\times n$ \cite{Bandeira2016} & Exact $d=n$ diagonal family: $\disc=(m-1)/\sqrt m$ against variance $1+1/m$, a ratio of $\Theta(\sqrt{\log n})$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:discrepancy}.\\
%
  % mixed-norm-general-s (body)
  \cxn & Sah--Sawhney--Stoner--Zhao mixed-norm Cauchy--Schwarz for all real $1\le p\le q$ \citeyearpar{SahSawhneyStonerZhao2020} & Nonnegative integer $3\times2$ blocks at $p=q=3/2$; deficit $64+64\sqrt2+30\sqrt{30}+17\sqrt{17}+29\sqrt{29}-42\sqrt{21}-160\sqrt5<0$ by exact algebra.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:mixed-general}.\\
%
  % rank-two-mixed-norm (body)
  \cxn & The same question at $B=A$: $\|Z\|_{s,q}^2\le\|Z\|_{s,s}\|Z\|_{q,q}$ for completely positive $Z$ & Explicit 21-vector Gram matrix and interval-separated ratio $>1$.\ \emph{Interval certificate}; no closed-form proof (reduction in Thm.~\ref{thm:mixed}).\\
%
  % quantum-coupon-collector (body)
  \cxn & The matrix coupon-collector sum $\sum_{\emptyset\ne S}(-1)^{|S|-1}X_S^{-1}$ was conjectured positive definite for every order $n$ and every dimension $d$. & Exact rational $3\times3$ matrices with $n=6$ and the integer vector $(4,1,3)^{\mathsf T}$ give a quadratic form strictly between $-96$ and $-95$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:quantum-coupon-collector}.\\
%
  % odonnell-matrix-conjecture (body)
  \cxn & Every absolute $c$ fails: an exact real-symmetric density-matrix family has ratio greater than $(\log_2 n)/32$. & An exact algebraic Givens-rotation family with a rational dyadic lower bound, accompanied by a standard-library exact-arithmetic verifier.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:odonnell-matrix-conjecture}.\\
%

  \cmidrule{2-3}\\
  & \multicolumn{2}{c}{\textsc{Items in the Appendix}}\\[5pt]
  \cmidrule{2-3}\\


  % dpp-feasible-step (appendix)
  \cxn & Mariet--Sra feasible-step Picard ascent conjecture for DPP likelihood \citeyearpar{MarietSra2015} & Exact rational $2\times2$ feasible update with lower likelihood.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:dpp}.\\
%
  % log-volume-distance (appendix)
  \cxn & Log-volume midpoint gap as a squared distance on convex bodies \cite{Shephard} & Strictly Lorentzian rational cubic realized as a Steiner polynomial in $\R^3$; exact rational triangle-violation bound $>7.15\times10^{-3}$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:volume-distance}.\\
%
  % lorentzian-jensen (appendix)
  \cxn & Lorentzian Jensen--Bregman metric principle: $\sqrt{\J_G}$ a semimetric for every homogeneous Lorentzian $G$ & The same cubic read as a homogeneous Lorentzian polynomial; the violation transfers verbatim.\ \emph{Exact certificate}; human proof Cor.~\ref{thm:lorentzian}.\\
%
  % sdd-nystrom-diminishing-returns (appendix)
  \cxn & A strictly diagonally dominant positive-definite $4\times4$ signed matrix has a nonempty-base marginal Nystr\"om error reduction that increases after an additional index is selected. & An exact integer $4\times4$ matrix, rational shift, four rational principal inverse traces, and the positive violation gap $2165/14833896$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:sdd-nystrom-diminishing-returns}.\\
%
  % osi-sketch-and-solve (appendix)
  \cxn & A one-dimensional oblivious subspace injection with injectivity $1$ can make sketch-and-solve incur the fixed factor $\sqrt2$ with probability $1/50$. & Three exact rationally weighted $2\times2$ sketch atoms, with exact isotropy, an exact $99/100$ injection guarantee, and exact squared residual ratios.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:osi-sketch-and-solve}.\\
%
  % hamiltonian-nepv-identity (appendix)
  \cxn & The proposed NEPv matrix does not reproduce the product-state Rayleigh quotient when the Hamiltonian sum includes a same-site term with $i=j$. & The certificate consists of exact $2\times2$ integer matrices and the integer vector $(1,2)^T$, for which the two assertedly equal values are $0$ and $-4/25$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:hamiltonian-nepv-identity}.\\
%
  % qrcp-orthonormal-greedy (appendix)
  \cxn & For an exact rational rank-three projector on eight coordinates, QRCP has strict pivots $I=(1,2,3)$ but the resulting inverse norm exceeds $\sqrt{18}$. & An exact rational $8\times3$ Grassmann-chart matrix, three positive rational pivot gaps, and the Rayleigh gap $192051/250000$.\ \emph{Exact certificate}; human proof Thm.~\ref{thm:qrcp-orthonormal-greedy}.\\
%
  \cmidrule[\heavyrulewidth]{2-3}
\end{longtable}
\setlength{\LTleft}{\fill}  % restore the default for later longtables



%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: "main"
%%% End:
